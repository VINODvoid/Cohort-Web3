//  sample example of backpack code for generating public and private key
// export function deriveEthereumWallet(
//     seed: Buffer,
//     derivationPath: string
//   ): Wallet {
//     const privateKey = deriveEthereumPrivateKey(seed, derivationPath);
//     return new Wallet(privateKey);
//   }
// important tip on web3 

// Always ensure that your private keys are securely stored and never exposed in your codebase.
// Use environment variables or secure vaults to manage sensitive information.